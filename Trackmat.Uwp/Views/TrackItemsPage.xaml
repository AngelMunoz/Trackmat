<Page
    x:Class="Trackmat.Uwp.Views.TrackItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:model="using:Trackmat.Lib.Models"
    mc:Ignorable="d">
    <Grid x:Name="ContentArea" Margin="{StaticResource SmallLeftRightMargin}">
        <Grid>
            <controls:DataGrid
                AutoGenerateColumns="False"
                GridLinesVisibility="Horizontal"
                ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Binding="{Binding Id}" Header="ID" />
                    <controls:DataGridTextColumn Binding="{Binding Item}" Header="Name" />
                    <controls:DataGridTextColumn Binding="{Binding Time}" Header="Time" />
                    <controls:DataGridTextColumn Binding="{Binding Url}" Header="Url" />
                    <controls:DataGridTextColumn Binding="{Binding StartDate}" Header="Start Date" />
                    <controls:DataGridTextColumn Binding="{Binding EndDate}" Header="End Date" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>
    </Grid>
</Page>
